title: The Love Story
description: "简洁的情侣小站，记录恋爱点滴与回忆。"
baseurl: "" # 如果作为项目站点进行部署，请设置仓库子路径如 "/my-repo"；如果部署到 user.github.io 或者本地静态部署，请留空
url: "https://alan-pluto.github.io" # 在部署前设置你的站点URL
markdown: kramdown
exclude:
  - node_modules
  - vendor
  - scripts

# Ensure dotfiles like .well-known are copied into the generated site
include:
  - .well-known
